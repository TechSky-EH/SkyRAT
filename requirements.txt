# SkyRAT Python Dependencies
# Install with: pip3 install -r requirements.txt

# Core networking and tunneling
pyngrok>=5.1.0              # Ngrok integration for external tunnels
requests>=2.28.0            # HTTP requests for API calls

# Command line interface and output
colorama>=0.4.4             # Cross-platform colored terminal output

# Development and debugging
setuptools>=65.0.0          # Package building and installation
wheel>=0.37.0               # Wheel package format

# Optional: Enhanced terminal features
rich>=12.0.0                # Rich text and beautiful formatting in terminal
click>=8.0.0                # Alternative command line interface framework

# Optional: Advanced networking
netifaces>=0.11.0           # Network interface detection
psutil>=5.9.0               # System and process monitoring

# Note: The following are built-in Python modules and don't need installation:
# - socket (networking)
# - threading (concurrent programming) 
# - subprocess (process management)
# - xml.etree.ElementTree (XML parsing)
# - json (JSON handling)
# - time (time functions)
# - os (operating system interface)
# - sys (system-specific parameters)
# - re (regular expressions)
# - shutil (file operations)
# - tempfile (temporary file handling)
# - pathlib (path handling - Python 3.4+)
# - base64 (base64 encoding/decoding)
# - argparse (command line argument parsing)